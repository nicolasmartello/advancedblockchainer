import { styled } from '@mui/material/styles';
import { Default } from './colors';

export const Header6 = styled('h6')(() => ({
  fontSize: '20px',
  color: Default.dark,
  margin: '0',
  fontWeight: 'normal',
}));